# Judicial Appeal Protocol v1.5.0
1. **Trigger**: Any modification to the `de2062` Merkle root.
2. **Threshold**: Requires 2-of-2 Ed25519 signature verification.
3. **Execution**: Must pass the `Scale Separation Gate` before commit.
4. **Log**: All judicial overrides are recorded in the global registry.
